# Copy to .env.local after running `npx convex dev`.
# Vite exposes only variables prefixed with VITE_.
VITE_CONVEX_URL=

# Convex server-side secrets (set with `npx convex env set NAME value`)
SCAMCHECK_API_KEY=
GOOGLE_SAFE_BROWSING_KEY=
VIRUSTOTAL_API_KEY=
GEMINI_API_KEY=
SARVAM_API_KEY=
